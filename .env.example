# ============================================
# NIS3351 智能家居监控系统 - 环境配置
# ============================================
# 复制此文件为 .env 并修改配置值：
#   cp .env.example .env

# 数据库类型（sqlite 或 opengauss）
DB_TYPE=sqlite

# ==================== SQLite 配置 ====================
# 仅当 DB_TYPE=sqlite 时使用
# SQLite 数据库文件路径（相对或绝对路径）
DB_PATH=data.sqlite3

# ==================== openGauss 配置 ====================
# 仅当 DB_TYPE=opengauss 时需要配置以下项

# 数据库连接信息
DB_HOST=127.0.0.1
DB_PORT=7654
DB_NAME=smart_home

# 数据库管理员账户（用于初始化数据库）
DB_ADMIN_USER=gaussdb
DB_ADMIN_PASSWORD=your_admin_password

# 应用程序使用的数据库用户（将由管理员账户创建）
DB_USER=your_username
DB_PASSWORD=your_password

# ==================== MQTT 配置 ====================
MQTT_BROKER=127.0.0.1
MQTT_PORT=1883
# 温湿度传感器主题（支持多设备，如：home/+/temperature_humidity）
MQTT_TOPIC=home/+/temperature_humidity
# 门锁主题
MQTT_LOCK_TOPIC=home/+/lock/#

# ==================== Flask 配置 ====================
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
DEBUG=false

# ==================== 前端配置 ====================
# 前端静态文件服务端口（http.server）
FRONTEND_PORT=8000

# ==================== 模拟器配置 ====================
# 温湿度传感器数据发送间隔（秒）
SENSOR_INTERVAL=5

# 烟雾报警器数据发送间隔（秒）
SMOKE_ALARM_INTERVAL=3

# 照明系统配置
# 房间亮度检测间隔（秒）
LIGHTING_CHECK_INTERVAL=30
# 自动开灯的房间亮度阈值（lux，低于此值时自动开灯）
LIGHTING_BRIGHTNESS_THRESHOLD=30

# ==================== 门锁配置 ====================
# 全局门锁PIN码（所有用户共享）
# 实际值存储在 pincode.json 中，通过前端 API 可修改
# 以下为备用默认值，仅当 pincode.json 不存在时使用
# GLOBAL_PINCODE=041117

# ==================== 安全提示 ====================
# ⚠️ 请勿将 .env 文件提交到 Git
# ⚠️ 生产环境请使用强密码
# ⚠️ .env 文件已在 .gitignore 中
